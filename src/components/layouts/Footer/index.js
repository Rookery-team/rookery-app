import styles from './index.module.scss'

const Footer = () => {

    return (
        <footer className={styles.footer}>
            <div className={styles.container}>
                <p>Créé dans le cadre d'un projet scolaire à l'école IPSSI</p>
            </div>
        </footer>
    )

}

export default Footer;

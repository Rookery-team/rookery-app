import React from 'react';
import styles from './index.module.scss';

const Watch = () => {
    return (<>
        <section>
            Une vidéo
        </section>
    </>);
};

export default Watch;
